# Streamlit Cloud installs THIS file automatically - it looks for
# `requirements.txt` at the repo root and ignores any other name, whatever the
# UI's "dependencies file" setting says. The first deploy failed because the
# full pipeline list lived here and app-store-scraper's requests==2.23.0 pin is
# unsatisfiable alongside google-genai.
#
# The pipeline's dependencies are in requirements-pipeline.txt and are installed
# explicitly:  pip install -r requirements-pipeline.txt
#
# The dashboard reads committed artifacts from data/dashboard/ and data/out/.
# It never scrapes and never calls an LLM, so none of the collection or provider
# libraries belong here - and leaving them out means the deployed app has no
# library capable of an outbound call.
streamlit>=1.36
pandas>=2.2
pyarrow>=16.0          # reads the parquet snapshots
plotly>=5.22           # every chart
pyyaml>=6.0            # reads config/taxonomy.yaml for area names
