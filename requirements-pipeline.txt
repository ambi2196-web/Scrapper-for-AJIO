# Full pipeline (S1-S9). Installed EXPLICITLY, never automatically:
#
#     pip install -r requirements-pipeline.txt
#
# Streamlit Cloud auto-installs whatever is named `requirements.txt` at the repo
# root and ignores any other file, whatever the UI setting says. So
# requirements.txt holds the seven dashboard packages and this file holds the
# pipeline - otherwise every deploy tries to install Playwright, both LLM SDKs
# and the scrapers, none of which the dashboard uses.

# --- core ---
pyyaml>=6.0
pydantic>=2.7
python-dotenv>=1.0

# --- data ---
pandas>=2.2
pyarrow>=16.0            # parquet from S5 onward; never CSV as an intermediate
statsmodels>=0.14        # Wilson intervals, two-proportion z-test
numpy>=1.26

# --- collection ---
google-play-scraper>=1.2.7
# app-store-scraper deliberately ABSENT. src/collect/appstore.py uses Apple's
# own RSS JSON feed over httpx instead, because the library is broken against
# urllib3 v2. It also pins requests==2.23.0, which is unsatisfiable alongside
# google-genai (requests>=2.28.1) - that conflict failed the first Cloud deploy.
praw>=7.7
google-api-python-client>=2.130   # YouTube Data API v3
httpx>=0.27
selectolax>=0.3.21       # faster than BeautifulSoup, same job
playwright>=1.44         # PDP fallback; run `playwright install chromium`

# --- text ---
pysbd>=0.3.4             # sentence boundaries; handles Indian-English punctuation

# --- LLM (free tiers only) ---
google-genai>=1.0        # lanes A and B
groq>=0.9                # lane C, the independent second annotator

# --- dev ---
pytest>=8.2
